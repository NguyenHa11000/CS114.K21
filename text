#include <iostream>

using namespace std;

int main()
{
    int a, b, c, d, e, f, m, n;
    cin>>a>>b>>c>>d>>e>>f;
    m = a*3600 + b*60 + c;
    n = d*3600 + e*60 +f;
    a= (m+n)/3600;
    b= ((m+n) - a*3600)/60;
    c= ((m+n)-a*3600)-b*60;
    cout<<a<<b<<c;

    return 0;
}
